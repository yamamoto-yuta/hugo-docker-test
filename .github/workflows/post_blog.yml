name: Post article to blog

on:
  push:
    branches:
      - develop-post-blog-workflow

jobs:
  post:
    runs-on: ubuntu-18.04
    steps:
      - name: Get issue
        run: |
          echo "test"
